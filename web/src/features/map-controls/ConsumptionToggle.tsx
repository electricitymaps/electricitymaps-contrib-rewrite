import ToggleButton from 'components/ToggleButton';
import type { ReactElement } from 'react';

interface ConsumptionToggleProperties {}

export default function ConsumptionToggle(
  properties: ConsumptionToggleProperties
): ReactElement {
  return <ToggleButton />;
}
